Add a new Automation feature: Scheduled Exports. Based on an export list and a domain,
an email is sent every X hours/days/weeks/months to a selection of users.

This module also provides **Export Groups** that allow grouping multiple scheduled
exports into a single email with multiple attachments. This is useful when you need to
send multiple related exports together, for example daily reporting exports to an
external system.
